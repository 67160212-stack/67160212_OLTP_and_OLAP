# รายงานแลป OLTP OLAP และ Pivot Table

**ชื่อ-นามสกุล:** [ชวนากร-การะบูรณ์]  
**รหัสนิสิต:** [67160212]  
**กลุ่มเรียน:** [1]  

---

## 1 OLTP

### 1.1 โค้ดและผลการรัน (oltp_demo.py)
* **โค้ดที่อัปเดต:**  
  `UPDATE orders SET status = 'PAID' WHERE order_id = 'O1004' AND status = 'PENDING'`
* **ผลรันรอบที่ 1:** `Updated rows (rowcount): 1` (อัปเดตสถานะสำเร็จ)
* **ผลรันรอบที่ 2:** `Updated rows (rowcount): 0` (ไม่มีแถวถูกแก้ไข)

### 1.2 คำอธิบาย
* **เหตุผลที่รอบสองแก้ไข 0 แถว และความสำคัญของเงื่อนไข status:**  
  เพราะสถานะของ `O1004` เปลี่ยนเป็น `PAID` ตั้งแต่การรันรอบแรกแล้ว เมื่อรันรอบที่สอง เงื่อนไข `status = 'PENDING'` จึงไม่ตรงกับแถวใดเลย การใส่เงื่อนไข `status` ช่วยป้องกันปัญหาการประมวลผลซ้ำซ้อน (Idempotency) และป้องกัน Race Condition ในระบบที่มีผู้ใช้งานพร้อมกันหลายคน
* **เหตุผลที่เป็น OLTP:**  
  เพราะเป็นการจัดการข้อมูลธุรกรรมระดับรายการ (Transaction Level) เน้นการปรับปรุงสถานะ (Update) ข้อมูล ณ เวลาปัจจุบันที่มีความถี่สูงและต้องรักษาสมบัติ ACID
* **ผลกระทบต่อ warehouse.db และกระบวนการเชื่อมข้อมูล:**  
  การเปลี่ยนสถานะใน `oltp.db` **ส่งผลทันทีไม่ได้** เนื่องจากฐานข้อมูล OLTP และ OLAP ถูกแยกออกจากกันเพื่อไม่ให้กระทบประสิทธิภาพการทำงาน ในระบบจริงต้องใช้กระบวนการ **ETL (Extract, Transform, Load)** หรือ Change Data Capture (CDC) เพื่อส่งผ่านข้อมูลจาก OLTP มายัง Data Warehouse แบบ Batch หรือ Real-time

---

## 2 Grain และ Star Schema

### 2.1 แผนภาพ Star Schema และ Grain
* **แผนภาพ Star Schema:**
  * **Fact Table:** `fact_sales` (PK: `order_id`, `line_no`)
  * **Dimension Tables:** `dim_date` (PK: `date_key`), `dim_product` (PK: `product_key`), `dim_store` (PK: `store_key`)
  * **ความสัมพันธ์:** `dim_date` (1) $\rightarrow$ `fact_sales` (N), `dim_product` (1) $\rightarrow$ `fact_sales` (N), `dim_store` (1) $\rightarrow$ `fact_sales` (N)
* **Grain (ความละเอียดข้อมูล):**  
  **1 แถวใน fact_sales หมายถึง รายการสินค้าแต่ละบรรทัดในใบเสร็จ (Line Item)** เช่น ออเดอร์ `O1001` มี 2 แถว ได้แก่ `O1001/line 1` (ซื้อ Tea) และ `O1001/line 2` (ซื้อ Cookie)

### 2.2 ผลตรวจข้อมูล (q01.sql)
* **จำนวนรายการ (line_count):** 8 รายการ
* **จำนวนออเดอร์ (order_count):** 6 ออเดอร์
* **จำนวนชิ้น (units):** 23 ชิ้น
* **ยอดขายรวม (revenue):** 1,390 บาท

### 2.3 Dimensions, Measures และ Hierarchy
* **3 Dimensions:** Date (เวลา), Product (สินค้า), Store (สถานที่)
* **2 Measures:** Quantity (จำนวนชิ้น), Amount (ยอดขายบาท)
* **Hierarchy เวลา:** Year $\rightarrow$ Month $\rightarrow$ Full Date
* **Hierarchy สถานที่:** Region $\rightarrow$ Province $\rightarrow$ Store Name
* **ทำไม unit_price ไม่ควรนำมา SUM:**  
  เพราะ `unit_price` เป็นราคาต่อหน่วย (Property) ไม่ใช่ผลคูณของปริมาณธุรกรรม การนำราคาต่อหน่วยมาบวกกันจะทำให้ได้ค่าที่ไม่คุ้มค่าต่อการตีความทางธุรกิจ (ต้องคำนวณเป็น `quantity * unit_price` ก่อนจึงจะเป็น Measure ที่ SUM ได้)

---

## 3 OLAP Query

### 3.1 สรุปผลลัพธ์ Query (q02 - q07, q12)
* **q02 (Roll-up รายเดือน):** 2026-08 = 490 บาท | 2026-09 = 900 บาท
* **q03 ( Roll-up เดือน + จังหวัด):** 
  * 2026-08: Bangkok = 310 บาท, Chonburi = 180 บาท
  * 2026-09: Bangkok = 540 บาท, Chonburi = 360 บาท
* **q04 (Drill-down รายวัน กันยายน):** 2026-09-09 = 360 บาท, 2026-09-10 = 300 บาท, 2026-09-11 = 240 บาท
* **q05 (Slice กันยายน รายจังหวัด):** Bangkok = 540 บาท | Chonburi = 360 บาท
* **q06 (Dice กันยายน, Drink, BKK/Chonburi):** Bangkok Drink = 300 บาท | Chonburi Drink = 200 บาท
* **q07 (HAVING ยอดขายกันยายน > 400):** Bangkok (540 บาท)
* **q12 (Drill-through Bangkok กันยายน):** 
  * `O1005` line 1: Tea 6 ชิ้น = 300 บาท
  * `O1006` line 1: Cookie 3 ชิ้น = 240 บาท

### 3.2 ตอบคำถามระหว่างทำ
* **ก. ความต่างของ q03 และ q04:**  
  q03 เพิ่มมิติมุมมองใหม่ (สถานที่/จังหวัด) เข้ามาเปรียบเทียบ ส่วน q04 เจาะลึกความละเอียดในมิติเดิม (จากรายเดือนเป็นรายวัน)
* **ข. การใช้ WHERE vs HAVING ใน q07:**  
  กรองเดือน (`month = '2026-09'`) ใน `WHERE` ก่อนการจัดกลุ่ม แล้วกรองผลรวมยอดขาย (`SUM(amount) > 400`) ใน `HAVING` หลังจัดกลุ่ม
* **ค. การตรวจสอบ q12 กับ q03:**  
  รายการจาก q12 รวมกันได้ $300 + 240 = 540$ บาท ซึ่งตรงกับช่อง `2026-09, Bangkok` ใน q03

---

## 4 Pivot Table

### 4.1 ผลการรัน SQL และ Python Pivot
* **q08 SQL Pivot (Province x Month):**  
  * Bangkok: Aug = 310, Sep = 540, Total = 850
  * Chonburi: Aug = 180, Sep = 360, Total = 540
* **P1 Grand Total:** 1,390 บาท
* **P2 สรุปหมวด Drink เดือนกันยายน:** ยอดรวม **500** บาท (Bangkok 300 บาท, Chonburi 200 บาท)
* **แกนภาพรวม:** Rows = province, Columns = month, Values = sum(amount)

### 4.2 การทดลองข้อผิดพลาด (aggfunc)
* **ลบ aggfunc:** ค่า Bangkok กันยายน กลายเป็น **270**
* **คำอธิบาย:** เกิดจาก pandas ใช้ค่าเริ่มต้นเป็น `mean` (ค่าเฉลี่ย) โดยยอดของ Bangkok กันยายน คือ 300 กับ 240 บาท ค่าเฉลี่ยจึงได้ $(300 + 240) / 2 = 270$ บาท ซึ่งไม่ใช่ยอดขายรวม

### 4.3 คำถามชวนคิด
* **เพิ่มเดือนตุลาคม:**  
  SQL ที่ใช้ `CASE WHEN` ต้องแก้ไขโค้ดเพื่อเพิ่มคอลัมน์ `oct` เองด้วยมือ ส่วน pandas ที่กำหนด `columns='month'` จะขยายเพิ่มคอลัมน์เดือนใหม่อัตโนมัติ (Dynamic)

---

## 5 ตรวจความถูกต้อง

### 5.1 ผลการรัน q09 - q11
* **q09 (UNION ALL):** ได้ยอดรวมเพิ่มเป็น 2 เท่าเนื่องจากนำแถวสรุปผลมารวมกับแถวข้อมูลปกติ
* **q10 (AOV & Avg Line):**
  * **AOV (Average Order Value):** 231.67 บาท/ออเดอร์
  * **Avg Line:** 173.75 บาท/รายการ
* **q11 (JOIN Check):** แถวและยอดขายรวมก่อนและหลัง JOIN ตรงกัน 100% (8 แถว / 1,390 บาท) และ `foreign_key_check` ไม่พบข้อผิดพลาด

### 5.2 คำอธิบาย AOV และชนิด Measure
* **AOV vs AVG(amount):**  
  AOV คือยอดซื้อเฉลี่ยต่อ 1 ใบเสร็จ (ออเดอร์) ส่วน AVG(amount) คือยอดซื้อเฉลี่ยต่อ 1 บรรทัดสินค้า AOV จึงมีค่าสูงกว่าเสมอ
* **ชนิดของ Measure:**
  1. **Additive:** `amount`, `quantity` (บวกข้ามมิติได้ทั้งหมด)
  2. **Semi-additive:** ยอดคงคลัง (บวกข้ามมิติสินค้าได้ แต่บวกข้ามมิติเวลาไม่ได้)
  3. **Non-additive:** `unit_price`, `AOV` (ห้ามนำมา SUM โดยตรง ต้องคำนวณจากตัวหารต้นทางใหม่)

---

## 6 สรุป

### 6.1 ข้อค้นพบจากข้อมูล 2 ข้อ
1. **ยอดขายมีการเติบโต:** ยอดขายรวมในเดือนกันยายน (900 บาท) เติบโตขึ้น 83.67% เมื่อเทียบกับเดือนสิงหาคม (490 บาท)
2. **กรุงเทพฯ เป็นตลาดหลัก:** พื้นที่ Bangkok สร้างรายได้คิดเป็น 61.15% (850 บาท) ของยอดขายรวมทั้งหมด

### 6.2 ข้อจำกัด 1 ข้อ
ชุดข้อมูลจำลองหลักมีขนาดเล็กมาก (มีเพียง 6 ออเดอร์ 8 รายการ) ทำให้ไม่สามารถนำไปใช้วิเคราะห์พฤติกรรมการซื้อซ้ำ หรือนำไปทำคำนวณเชิงสถิติขั้นสูงที่มีความน่าเชื่อถือสูงได้

### 6.3 การใช้งาน AI
* **Prompt ที่ใช้:** "ช่วยตรวจสอบ SQL query และเขียนคำสั่ง pandas pivot table ตามโจทย์แลป"
* **จุดที่ตรวจแก้ด้วยตนเอง:** ตรวจสอบการแปลงประเภทข้อมูลตัวหารใน SQL (`CAST(SUM(amount) AS FLOAT)`) เพื่อไม่ให้ SQLite ตัดเศษทศนิยมทิ้ง